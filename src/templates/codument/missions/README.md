# codument/missions

这个目录由 `codument-*-mission` 系列命令使用。

- `codument-plan-mission` 在这里创建 mission。
- `codument-impl-mission` 在这里读取和更新 mission 状态。
- `codument-archive-mission` 在这里归档完成或取消的 mission。

一般不要手工维护本目录结构；需要长期任务时通过对应 mission 命令操作。
