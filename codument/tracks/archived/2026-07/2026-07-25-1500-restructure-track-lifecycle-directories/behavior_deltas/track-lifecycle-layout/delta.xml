<behavior-patch capability="track-lifecycle-layout" version="1">
  <upsert selector="behavior://track-lifecycle-layout/requirements/unified-lifecycle-directories">
    <requirement id="unified-lifecycle-directories">
      <statement>Codument SHALL store pending, active, and archived tracks below codument/tracks and SHALL migrate legacy workspace paths during upgrade without overwriting conflicts.</statement>
      <suite id="workspace-upgrade" name="workspace upgrade">
        <case id="moves-legacy-layout">
          <given>a workspace contains direct legacy active tracks and a legacy codument/archive tree</given>
          <when>the user runs codument upgrade-workspace</when>
          <then>the tracks SHALL move into tracks/active and tracks/archived after backup creation</then>
        </case>
        <case id="preserves-conflicts">
          <given>a lifecycle destination already exists</given>
          <when>the user runs codument upgrade-workspace</when>
          <then>the command SHALL preserve the legacy source and report the conflict</then>
        </case>
      </suite>
    </requirement>
  </upsert>
</behavior-patch>
