# Track：nested-mission-operational-docs

## 目标

把 Nested Mission、MissionLink、selected-tasks、跨层 TrackLink 和 `codument project bind` 的使用与恢复流程写入本项目标准操作文档。

## 验收

文档覆盖创建、绑定、跨机器切换、UNBOUND/MISSING/DRIFTED/BLOCKED 和子 Mission 自治边界。
