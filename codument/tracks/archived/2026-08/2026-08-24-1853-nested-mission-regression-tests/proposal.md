# Track：nested-mission-regression-tests

## 目标

为 MissionLink、TrackLink mission_ref/track_ref、WorkspaceBinding CLI 和旧 Mission 兼容行为增加回归验证。

## 验收

focused Mission validator tests、typecheck 和 workspace binding CLI smoke 均通过。
