# Track Design：Nested Mission Regression Tests

以现有 `test/cli/mission/validate.test.ts` 为基线，补充新字段和旧 TrackLink 兼容测试；用临时 HOME/workspace 验证 project bind/bindings/unbind 读写和 Git ignore；运行 typecheck、focused tests、严格资源校验。
