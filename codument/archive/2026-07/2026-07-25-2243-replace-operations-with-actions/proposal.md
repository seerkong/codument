# Change: replace operations with actions

Replace the release surface `operations` with `actions`, including hook configuration, prompt terminology, CLI help, and workspace upgrade. Establish `commands`, `protocols`, `methods`, and `compat` as distinct standard owners.

Non-goal: preserve `operation` only inside migration compatibility and historical evidence.
